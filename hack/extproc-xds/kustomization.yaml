apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aibrix-system
namePrefix: aibrix-
resources:
  - ../../config/gateway

# This overlay exists only to render config/gateway the way config/default does
# (its namePrefix and namespace), so the ext_proc xDS golden is generated from
# the names that are actually deployed. See README.md.
